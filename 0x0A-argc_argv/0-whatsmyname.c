#include "main.h"
#include <stdio.h>
/**
 * main - prints its name
 * @argc: no of arg
 * @argv: array of arg
 * Return: 0
 */
int main(int argc, char *argv[])

{

(void) argc;
printf("%s\n", argv[0]);
return (0);
}
